apiVersion: v1
kind: Service
metadata:
  name: {{ include "crypto-recommender.fullname" . }}
  labels:
    app: {{ include "crypto-recommender.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    app: {{ include "crypto-recommender.name" . }}
